({
    calloutCtrl : function(component, event, helper) {
  
        // Search Text needs to be collected from search component
        var base = 'J2EE';
        helper.getResponse(component, base);
    },
 
})